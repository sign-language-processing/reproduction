# Training modifications

To demonstrate we can use this Dockerfile to train a model, 
we provide `patch.sh` to modify the original training scripts with a few updates.

You can edit `batch_size` in configs/experiments/cosmos_tokenize1.py.